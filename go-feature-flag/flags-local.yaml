generation:
  variations:
    enabled: true
    disabled: false
  targeting:
    - query: (targetingKey eq "terminal-1") and (currency eq "USD") and (amount > 50)
      variation: enabled
  defaultRule:
    variation: disabled