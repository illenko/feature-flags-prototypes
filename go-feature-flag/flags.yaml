test-flow:
  variations:
    enabled: true
    disabled: false
  targeting:
    - query: (targetingKey eq "terminal-1") and (currency eq "USD") and (amount > 100)
      variation: enabled
  defaultRule:
    variation: disabled